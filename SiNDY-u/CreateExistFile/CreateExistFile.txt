【CheckExistFile 使用方法】	2007/11/15 H.Hyodo

■モジュール名

	CheckExistFile

■機能

	存在判定ファイルの作成

■オプション

	-a
		全都市地図メッシュについて生成

	-l <メッシュリストファイル>
		メッシュリストファイルで生成対象メッシュを指定

	-m <メッシュコード>
		指定コードの都市地図メッシュのみ生成

	-o <ディレクトリ>
		存在判定ファイルの出力先ディレクトリ指定
		（環境変数 EXIST_DIR でも指定可）

	-d <分割数>
		存在判定を行うエリア単位を都市地図メッシュの分割数で指定
		デフォルトは 32／設定可能範囲は 2～64

　※オプションは -a / -l / -m のいずれかが必須。

■環境変数

	EXIST_DIR
		存在判定ファイルの出力先ディレクトリ指定

	DB_MAP
		GeoDB接続文字列を指定（都市地図背景系サーバ）

	FC_CITYSITE
		都市地図背景レイヤのフィーチャクラス名を指定

	FC_CITYMESH
		都市地図メッシュレイヤのフィーチャクラス名を指定

※GeoDB接続文字列の書式：
	<ユーザ>/<パスワード>/<バージョン>/<インスタンス>/<サーバ名>

■更新履歴

	2002/06/11？
		新規作成。（以降、何度か更新？）
	2003/10/31
		更新？
	(2004/04/12)
		地図制作Ｇに公開
	2006/09/05
		Arc9.1に対応
	2007/11/15	FV: 1.2.1.5 / PV: 1.2.1.0
		・VC8対応。
		・[Bug 5650] CityMeshのメッシュコードフィールド名を「MeshCode」に変更。
		・対象メッシュの検索で「Complete_f is not null」を条件から除外。
		　（オプション「-l」「-m」指定時）
		・進捗表示を標準出力(stdout)に、エラー表示を標準エラー出力(stderr)に変更。

