/*
 * Copyright (C) INCREMENT P CORP. All Rights Reserved.
 *
 * THIS SOFTWARE IS PROVIDED BY INCREMENT P CORP., WITHOUT WARRANTY OF
 * ANY KIND, EXPRESS OR IMPLIED, INCLUDING BUT NOT LIMITED TO THE
 * WARRANTIES OF MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE
 * AND NONINFRINGEMENT.
 *
 * IN NO EVENT SHALL THE AUTHORS OR COPYRIGHT HOLDER BE LIABLE FOR ANY
 * CLAIM, DAMAGES SUFFERED BY LICENSEE AS A RESULT OF USING, MODIFYING
 * OR DISTRIBUTING THIS SOFTWARE OR ITS DERIVATIVES.
*/

#pragma once
static const int V_MARGIN = 3;

/// ウィンドウマネージャ配置マップ（基本タブ）
BEGIN_WINDOW_MAP(WALKLINK_KIHON_MAP)
	BEGINROWS(WRCT_REST,0,RCMARGINS(2,2))
		BEGINROWS(WRCT_REST, IDC_TAB_WALKLINK, RCMARGINS(2, 2))
			RCSPACE(20)
			BEGINROWS(WRCT_REST,0,0)
				RCSPACE(V_MARGIN)
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					RCTOFIT(IDC_STATIC_WALKCLASS)
					RCTOFIT(-1)
					RCREST(IDC_COMBO_WALKCLASS)
				ENDGROUP()
				RCSPACE(V_MARGIN)
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					RCTOFIT(IDC_STATIC_ROADCODE)
					RCREST(IDC_EDIT_ROADCODE)
					RCTOFIT(-1)
					RCTOFIT(IDC_BUTTON_SEARCHROADCODE)
				ENDGROUP()
				RCSPACE(V_MARGIN)
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					RCTOFIT(IDC_STATIC_ROADNAME)
					RCTOFIT(-1)
					RCREST(IDC_STATIC_ROADNAME2)
				ENDGROUP()
				RCSPACE(5)
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					RCTOFIT(IDC_CHECK_AUTOWALK)
					RCSPACE(5)
					RCTOFIT(IDC_CHECK_STAIR)
					RCSPACE(5)
					RCTOFIT(IDC_CHECK_SLOPE)
					RCSPACE(5)
					RCTOFIT(IDC_CHECK_TUNNEL)
				ENDGROUP()
				RCSPACE(5)
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					RCTOFIT(IDC_CHECK_ESCALATOR)
					RCSPACE(5)
					RCTOFIT(IDC_CHECK_ARCADE)
					RCSPACE(5)
					RCTOFIT(IDC_CHECK_OPEN_AREA)
					RCSPACE(5)
					RCTOFIT(IDC_CHECK_SCRAMBLE)
				ENDGROUP()
				RCSPACE(5)
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					BEGINROWS(WRCT_PCT, 0, 50)
						BEGINCOLS(WRCT_REST, 0, 0)
							RCTOFIT(IDC_STATIC_FLOORLEVEL)
							RCTOFIT(-1)
							RCREST(IDC_EDIT_FLOORLEVEL)
						ENDGROUP()
					ENDGROUP()
					RCSPACE(10)
					BEGINROWS(WRCT_PCT, 0, 50)
						BEGINCOLS(WRCT_REST, 0, 0)
							RCTOFIT(IDC_STATIC_PLATFORM)
							RCTOFIT(-1)
							RCREST(IDC_EDIT_PLATFORM)
						ENDGROUP()
					ENDGROUP()
				ENDGROUP()
				RCSPACE(V_MARGIN)
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					RCTOFIT(IDC_STATIC_FLOORMOVE)
					RCTOFIT(-1)
					RCREST(IDC_COMBO_FLOORMOVE)
				ENDGROUP()
				RCSPACE(V_MARGIN)
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					RCTOFIT(IDC_STATIC_MATERIAL)
					RCTOFIT(-1)
					RCREST(IDC_COMBO_MATERIAL)
				ENDGROUP()
				RCSPACE(V_MARGIN)
				BEGINCOLS(WRCT_REST, 0, RCMARGINS(10,0))      // 道路リンクID
					RCTOFIT(IDC_STATIC_ROADID)
					RCTOFIT(-1)
					RCTOFIT(IDC_LIST_ROAD)
						BEGINROWS(WRCT_TOFIT,0,RCMARGINS(10,10))              // <、>ボタン
							RCTOFIT(IDC_BUTTON_ROAD_ACCEPT)
							RCSPACE(V_MARGIN)
							RCTOFIT(IDC_BUTTON_ROAD_REMOVE)
						ENDGROUP()
						BEGINROWS(WRCT_REST,0,RCMARGINS(0,0))              // 検索結果リスト、範囲
							RCREST(IDC_LIST_ROADSEARCH)
							RCSPACE(3)
							BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(0,0))
								RCTOFIT(IDC_STATIC_SEARCHRANG)
								RCREST(IDC_EDIT_SEARCHRANGE)
								RCTOFIT(IDC_BUTTON_SEARCH)
							ENDGROUP()
						ENDGROUP()
				ENDGROUP()
			ENDGROUP()
			BEGINROWS(WRCT_TOFIT, 0, RCMARGINS(4,4))
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(0,0))
					RCREST(IDC_STATIC_VAR)
				ENDGROUP()
				RCSPACE(5)
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					RCTOFIT(IDC_STATIC_SOURCE)
					RCTOFIT(-1)
					RCREST(IDC_EDIT_SOURCE)
				ENDGROUP()
			ENDGROUP()
		ENDGROUP()
	ENDGROUP()
END_WINDOW_MAP()

/// ウィンドウマネージャ配置マップ(規制タブ)
BEGIN_WINDOW_MAP(WALKLINK_KISEI_MAP)
	BEGINROWS(WRCT_REST,0,RCMARGINS(2,2))
		BEGINROWS(WRCT_REST, IDC_TAB_WALKLINK, RCMARGINS(2, 2))
		RCSPACE(30)
		RCTOFIT(-1)
			BEGINROWS(WRCT_REST, 0, RCMARGINS(10,2))
				BEGINROWS(WRCT_PCT, IDC_STATIC_ONEWAYGROUP, 50)
					RCSPACE(30)
					BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
						RCTOFIT(IDC_STATIC_REGULERONEWAY)
						RCTOFIT(-1)
						RCREST(IDC_COMBO_ONEWAY)
					ENDGROUP()
					RCSPACE(5)
					RCTOFIT(-1)
					BEGINCOLS(WRCT_REST, 0, RCMARGINS(10,0))
						RCREST(IDC_GRID_ONEWAY)
					ENDGROUP()
					RCSPACE(5)
				ENDGROUP()
				RCSPACE(V_MARGIN)
				BEGINROWS(WRCT_PCT, IDC_STATIC_NOPASSAGEGROUP, 50)
					RCSPACE(30)
					BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))	// 通常通行禁止
						RCTOFIT(IDC_STATIC_REGULERNOPASSAGE)
						RCTOFIT(-1)
						RCREST(IDC_COMBO_NOPASSAGE)
					ENDGROUP()
					RCSPACE(5)
					RCTOFIT(-1)
					BEGINCOLS(WRCT_REST, 0, RCMARGINS(10,0))
						RCREST(IDC_GRID_NOPASSAGE)
					ENDGROUP()
					RCSPACE(5)
				ENDGROUP()
			ENDGROUP()
		ENDGROUP()
			BEGINROWS(WRCT_TOFIT, 0, RCMARGINS(4,4))
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(0,0))
					RCREST(IDC_STATIC_VAR)
				ENDGROUP()
				RCSPACE(5)
				BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					RCTOFIT(IDC_STATIC_SOURCE)
					RCTOFIT(-1)
					RCREST(IDC_EDIT_SOURCE)
				ENDGROUP()
			ENDGROUP()
	ENDGROUP()
END_WINDOW_MAP()
