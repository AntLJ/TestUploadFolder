/**
@page overview 機能概要

SiNDY-c用のPGDBの編集差分をSiNDYのデータベースに反映する。
現時点では一時停止限定版と一時停止以外版でバイナリが異なる。
基本的は使用法、動作はほぼ同じであるため、特に断りがない限り
共通のことを指す。

- 特記事項
  - <b>一時停止に未対応</b>
  - メッシュ境界に関連するオブジェクトは更新対象外。
  - 実行中に大文字「P」を押すことで、区切りのいいところでサーバから切断し、処理を一時中断することができます。
  - 中断中に大文字「S」を押すことで、続きから処理できます。
  - 実行中又は中断中に大文字「Q」を押すことで、そこまでの処理を保存して終了することができます（但し、再開する場合は要注意）

*/
