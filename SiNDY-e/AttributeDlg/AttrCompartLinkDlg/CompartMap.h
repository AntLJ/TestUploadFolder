/*
 * Copyright (C) INCREMENT P CORP. All Rights Reserved.
 *
 * THIS SOFTWARE IS PROVIDED BY INCREMENT P CORP., WITHOUT WARRANTY OF
 * ANY KIND, EXPRESS OR IMPLIED, INCLUDING BUT NOT LIMITED TO THE
 * WARRANTIES OF MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE
 * AND NONINFRINGEMENT.
 *
 * IN NO EVENT SHALL THE AUTHORS OR COPYRIGHT HOLDER BE LIABLE FOR ANY
 * CLAIM, DAMAGES SUFFERED BY LICENSEE AS A RESULT OF USING, MODIFYING
 * OR DISTRIBUTING THIS SOFTWARE OR ITS DERIVATIVES.
*/

///  ウィンドウマネージャ配置マップ（基本属性タブ用）
BEGIN_WINDOW_MAP(KIHONMAP)
	BEGINROWS(WRCT_REST,0,RCMARGINS(2,2))
        BEGINROWS(WRCT_PCT,0,90)
	      BEGINROWS(WRCT_REST,IDC_TAB_COMPARTATTR,0)
	      RCSPACE(22)	// タブのつまみ分			
		   BEGINROWS(WRCT_REST,0,0)
			BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
				RCTOFIT(IDC_STATIC_COMPART_LINE_C)
				RCTOFIT(-1)
				RCREST(IDC_COMBO_COMPART_LINE_C)
			ENDGROUP()
			RCSPACE(10)
			BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
				RCTOFIT(IDC_STATIC_COMPART_STYLE_C)
				RCTOFIT(-1)
				RCREST(IDC_COMBO_COMPART_STYLE_C)
			ENDGROUP()
			RCSPACE(10)
			BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
				RCTOFIT(IDC_STATIC_SLOWDOWN_LABEL_C)
				RCTOFIT(-1)
				RCREST(IDC_COMBO_SLOWDOWN_LABEL_C)
			ENDGROUP()
			RCSPACE(10)
			BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
				RCREST(IDC_CHECK_INSTALLATION_F)
			ENDGROUP()
			RCSPACE(10)
			BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
				RCTOFIT(IDC_STATIC_SPECULATION_C)
				RCTOFIT(-1)
				RCREST(IDC_COMBO_SPECULATION_C)
			ENDGROUP()
			RCSPACE(10)
			BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
				RCTOFIT(IDC_STATIC_SOURCE_ID)
				RCTOFIT(-1)
				RCREST(IDC_EDIT_SOURCE_ID)
			ENDGROUP()
			RCSPACE(10)
			BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
				RCTOFIT(IDC_STATIC_LANE_LINK)
				RCTOFIT(-1)
				RCREST(IDC_LIST_LANE_LINK)
			ENDGROUP()
			RCSPACE(10)
		   ENDGROUP()			
		  ENDGROUP()
		ENDGROUP()			
		//情報ソース
		BEGINROWS(WRCT_PCT, 0, 10)
			RCSPACE(3)
			BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
				RCREST(IDC_STATIC_VAR)
			ENDGROUP()
			RCSPACE(3)
			BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
				RCTOFIT(IDC_STATIC_SOURCE)
				RCTOFIT(-1)
				RCREST(IDC_EDIT_SOURCE)
			ENDGROUP()
		ENDGROUP()
	ENDGROUP()	
END_WINDOW_MAP()

/// ウィンドウマネージャ配置マップ（ノード列属性タブ用）
BEGIN_WINDOW_MAP(NODEMAP)
	BEGINROWS(WRCT_REST,0,RCMARGINS(2,2))
	  BEGINROWS(WRCT_PCT,0,90)
		BEGINROWS(WRCT_REST,IDC_TAB_COMPARTATTR,0)
			RCSPACE(22)	// タブのつまみ分			
			BEGINROWS(WRCT_REST, 0, RCMARGINS(10,2))
					BEGINCOLS(WRCT_TOFIT, 0, RCMARGINS(10,0))
					    RCTOFIT(IDC_STATIC_NODELIST_INFO)
				    ENDGROUP()
				    RCSPACE(3)					
					RCTOFIT(-1)
					BEGINCOLS(WRCT_REST, 0, RCMARGINS(10,0))
						RCREST(IDC_GRID_NODELIST)
					ENDGROUP()							
			ENDGROUP()
			RCSPACE(4)
		ENDGROUP()
	ENDGROUP()
	BEGINROWS(WRCT_PCT,0,10)		
		BEGINROWS(WRCT_TOFIT, 0, RCMARGINS(4,4))
			BEGINCOLS(WRCT_TOFIT, 0, 0)
				RCTOFIT(IDC_STATIC_SOURCE)
				RCTOFIT(-1)
				RCREST(IDC_EDIT_SOURCE)
			ENDGROUP()
		ENDGROUP()
	ENDGROUP()
  ENDGROUP()
END_WINDOW_MAP()